import { DriverContext } from '@beemo/core';
import { Command, GlobalOptions, PrimitiveType } from '@boost/cli';
import { SerialPipeline } from '@boost/pipeline';
export declare abstract class BaseRunCommand<O extends object, P extends PrimitiveType[], C extends object = {}> extends Command<GlobalOptions & O, P, C> {
    concurrency: number;
    graph: boolean;
    workspaces: string;
    renderDriver(pipeline: SerialPipeline<{}, DriverContext, unknown, unknown>): Promise<void>;
}
//# sourceMappingURL=BaseRunCommand.d.ts.map