'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

var common = require('@boost/common');

var translate = require('../translate.js');

var blueprints = require('./blueprints.js');

function validateParams(params) {
  params.forEach((config, index) => {
    var _config$label;

    return common.optimal(config, blueprints.paramBlueprint, {
      name: translate.msg('cli:labelParam', {
        name: (_config$label = config.label) !== null && _config$label !== void 0 ? _config$label : index
      }),
      unknown: false
    });
  });
}

exports.validateParams = validateParams;
//# sourceMappingURL=validateParams.js.map
