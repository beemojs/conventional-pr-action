'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

var isNodeBinary = require('./isNodeBinary.js');

function isArgvSize(argv, size) {
  let list = [...argv]; // Node process binaries are included

  if (list.length > 0 && isNodeBinary.isNodeBinary(list[0])) {
    list = list.slice(2);
  } // Rest args are included


  list.some((arg, index) => {
    if (arg === '--') {
      list = list.slice(0, index);
      return true;
    }

    return false;
  });
  return list.length === size;
}

exports.isArgvSize = isArgvSize;
//# sourceMappingURL=isArgvSize.js.map
