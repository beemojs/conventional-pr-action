'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

var terminal = require('@boost/terminal');

function getLongestWidth(values) {
  return values.reduce((sum, value) => {
    const text = terminal.stripAnsi(value);
    return text.length > sum ? text.length : sum;
  }, 0);
}

exports.getLongestWidth = getLongestWidth;
//# sourceMappingURL=getLongestWidth.js.map
