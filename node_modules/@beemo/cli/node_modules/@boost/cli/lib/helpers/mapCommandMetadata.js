'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

function mapCommandMetadata(commands) {
  const map = {};
  Object.entries(commands).forEach(([path, config]) => {
    map[path] = config.getMetadata();
  });
  return map;
}

exports.mapCommandMetadata = mapCommandMetadata;
//# sourceMappingURL=mapCommandMetadata.js.map
