'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

var terminal = require('@boost/terminal');

var loadTheme = require('./loadTheme.js');

function applyStyle(text, type) {
  const color = loadTheme.loadTheme()[type];
  const apply = color.startsWith('#') ? terminal.style.hex(color) : terminal.style[color];
  return apply(text);
}

exports.applyStyle = applyStyle;
//# sourceMappingURL=applyStyle.js.map
