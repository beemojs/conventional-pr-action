import { ConfigContext, Driver, Path } from '@beemo/core';
import { Event } from '@boost/event';
import { PrettierConfig } from './types';
export declare class PrettierDriver extends Driver<PrettierConfig> {
    readonly name = "@beemo/driver-prettier";
    readonly onCreateIgnoreFile: Event<[ConfigContext<{}>, Path, {
        ignore: string[];
    }], string>;
    bootstrap(): void;
    /**
     * If an "ignore" property exists in the Prettier config, create an ".prettierconfig" file.
     */
    private handleCreateIgnoreFile;
}
//# sourceMappingURL=PrettierDriver.d.ts.map