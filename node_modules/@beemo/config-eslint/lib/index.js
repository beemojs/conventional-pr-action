'use strict';

const configConstants = require('@beemo/config-constants');

const tool = process.beemo.tool;
const _tool$config$settings = tool.config.settings,
      node = _tool$config$settings.node,
      react = _tool$config$settings.react;
const config = {
  root: true,
  extends: ['beemo'],
  ignore: [...configConstants.IGNORE_LIST, '*.min.js', '*.map', '*.snap']
};

if (react) {
  config.extends.push('beemo/react');
}

if (node) {
  config.extends.push('beemo/node');
} else {
  config.extends.push('beemo/browser');
}

module.exports = config;
//# sourceMappingURL=index.js.map
