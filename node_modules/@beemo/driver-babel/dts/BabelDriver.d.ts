import { Driver } from '@beemo/core';
import { BabelConfig } from './types';
export declare class BabelDriver extends Driver<BabelConfig> {
    readonly name = "@beemo/driver-babel";
    bootstrap(): void;
    extractErrorMessage(error: {
        message: string;
    }): string;
    /**
     * Automatically clean the target folder if --out-dir is used.
     */
    private handleCleanTarget;
}
//# sourceMappingURL=BabelDriver.d.ts.map