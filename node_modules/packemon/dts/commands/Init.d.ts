/// <reference types="react" />
import { WorkspacePackage } from '@boost/common';
import { PackemonPackage, PackemonPackageConfig } from '../types';
import { BaseCommand } from './Base';
export interface InitOptions {
    force: boolean;
    skipPrivate: boolean;
}
export declare class InitCommand extends BaseCommand<InitOptions> {
    force: boolean;
    run(): Promise<void | JSX.Element>;
    formatConfigObject({ format, inputs, namespace, platform, support, }: PackemonPackageConfig): PackemonPackageConfig;
    writeConfigsToPackageJsons(packages: WorkspacePackage<PackemonPackage>[], configs: Record<string, PackemonPackageConfig>): Promise<void>;
}
//# sourceMappingURL=Init.d.ts.map