'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

const React = require('react');

const ink = require('ink');

const react = require('@boost/cli/react');

const terminal = require('@boost/terminal');

const constants = require('../constants.js');

const _interopDefault = e => e && e.__esModule ? e : {
  default: e
};

const React__default = /*#__PURE__*/_interopDefault(React);

function Target({
  target,
  children,
  state = 'pending'
}) {
  var _constants$STATE_COLO;

  return /*#__PURE__*/React__default.default.createElement(ink.Box, {
    marginLeft: 1
  }, /*#__PURE__*/React__default.default.createElement(react.Style, {
    bold: true,
    type: (_constants$STATE_COLO = constants.STATE_COLORS[state]) !== null && _constants$STATE_COLO !== void 0 ? _constants$STATE_COLO : 'default'
  }, state === 'failed' ? terminal.figures.cross : terminal.figures.squareSmallFilled, " ", target.toLowerCase()), children);
}

exports.Target = Target;
//# sourceMappingURL=Target.js.map
