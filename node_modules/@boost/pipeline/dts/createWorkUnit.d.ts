import { Action } from './types';
import { WorkUnit } from './WorkUnit';
/**
 * Create or return an executable work unit. Supports the following patterns:
 *
 * - A `Routine` instance.
 * - A `Task` instance.
 * - A custom `WorkUnit` instance.
 * - A title and function that returns a `Task` instance.
 */
export declare function createWorkUnit<Input = unknown, Output = Input>(titleOrWorkUnit: WorkUnit<{}, Input, Output> | string, action?: Action<any, Input, Output>, scope?: unknown): WorkUnit<{}, Input, Output>;
//# sourceMappingURL=createWorkUnit.d.ts.map