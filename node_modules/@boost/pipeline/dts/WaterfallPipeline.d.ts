import { Context } from './Context';
import { SerialPipeline } from './SerialPipeline';
export declare class WaterfallPipeline<Ctx extends Context, Input = unknown> extends SerialPipeline<{}, Ctx, Input> {
    /**
     * Execute the pipeline in sequential order with the output of each
     * work unit being passed to the next work unit in the chain.
     */
    run(): Promise<Input>;
}
//# sourceMappingURL=WaterfallPipeline.d.ts.map