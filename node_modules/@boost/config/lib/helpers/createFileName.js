'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

function createFileName(name, ext, options) {
  const envSuffix = options.envSuffix,
        leadingDot = options.leadingDot;
  let fileName = name;

  if (leadingDot) {
    fileName = `.${name}`;
  }

  if (envSuffix) {
    fileName += `.${envSuffix}`;
  }

  fileName += `.${ext}`;
  return fileName;
}

exports.createFileName = createFileName;
//# sourceMappingURL=createFileName.js.map
