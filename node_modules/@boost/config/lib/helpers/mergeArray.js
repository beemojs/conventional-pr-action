'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
/**
 * Merges previous and next arrays into a new array while removing duplicates (using `Set`).
 */

function mergeArray(prev, next) {
  return [...new Set([...prev, ...next])].filter(value => value !== undefined);
}

exports.mergeArray = mergeArray;
//# sourceMappingURL=mergeArray.js.map
