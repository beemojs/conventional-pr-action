'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
/**
 * Detect the current environment name from `<project>_ENV` and `NODE_ENV`.
 */

function getEnv(name) {
  var _ref, _process$env$;

  const constName = name.replace(/[A-Z]/gu, char => `_${char}`).toUpperCase();
  return ((_ref = (_process$env$ = process.env[`${constName}_ENV`]) !== null && _process$env$ !== void 0 ? _process$env$ : process.env.NODE_ENV) !== null && _ref !== void 0 ? _ref : 'development').toLowerCase();
}

exports.getEnv = getEnv;
//# sourceMappingURL=getEnv.js.map
