import { Contract } from '@boost/common';
import { Pluggable } from './types';
export declare abstract class Plugin<T = unknown, Options extends object = {}> extends Contract<Options> implements Pluggable<T> {
    abstract name: string;
    startup(tool: T): void;
    shutdown(tool: T): void;
}
//# sourceMappingURL=Plugin.d.ts.map